# JULE Browser for iOS — Source 1.0

Create a new SwiftUI iOS project in Xcode, replace the generated Swift files with these files, and add `JULELogo.png` to Assets as `JULELogo`.

Apple signing is required to install on iPhone. The app uses the platform-required WKWebView while preserving the JULE interface and in-frame navigation.
